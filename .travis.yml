services:
  - mysql
language: php
php:
    - '7.0'
before_install:
  - sudo apt-get update
  
  # database
  - mysql -u root < server/database/SQL/projetx.sql
  
  # apache server 
  - sudo apt-get install apache2 libapache2-mod-fastcgi
  
  # configure apache virtual hosts
  - sudo cp -f .travis-ci-apache /etc/apache2/sites-available/default
  - sudo sed -e "s?%TRAVIS_BUILD_DIR%?$(pwd)?g" --in-place /etc/apache2/sites-available/default
  - sudo service apache2 restart
  
before_script: composer require atoum/atoum
script: vendor/bin/atoum

script:
    - php server/tests/atoum.phar
repo_token: qIapEkNDgw3Gjr4Z23S59PHXjUHONoZVy