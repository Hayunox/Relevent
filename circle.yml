machine:
  php:
    version: 7
dependencies:
  post:
    - sudo cp ~/projetX/circle-server-config /etc/apache2/sites-available
    - sudo a2ensite circle-server-config
    - sudo service apache2 restart